<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Definir un tema claro para la aplicación -->
    <style name="Theme.App.Light" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Color de fondo blanco para toda la app -->
        <item name="android:windowBackground">@android:color/white</item>

        <!-- Color del texto y elementos -->
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textColorSecondary">@android:color/darker_gray</item>

        <!-- Color del ActionBar o Toolbar -->
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorPrimaryDark">@android:color/black</item>
        <item name="colorAccent">@android:color/black</item>

        <!-- Otras configuraciones del tema claro -->
    </style>
</resources>